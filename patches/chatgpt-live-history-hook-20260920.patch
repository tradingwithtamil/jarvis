--- a/main.py
+++ b/main.py
@@
 from skills.skill_manager       import format_skill_index_for_prompt, load_constitution_for_prompt
+from chatgpt_history             import append_api_turn
@@
             self.ui.write_log(f"{self._asst_name}: {answer}")
             self._session_log.append(f"{self._asst_name}: {answer}")
             self._human.observe_assistant(answer)
+            try:
+                await asyncio.to_thread(
+                    append_api_turn,
+                    user_text,
+                    answer,
+                    session_id="jarvis-final-text",
+                )
+            except Exception as exc:
+                print(f"[ChatGPTHistory] Final text sync skipped: {exc}")
@@
                             if full_out:
                                 self.ui.write_log(f"{self._asst_name}: {full_out}")
                                 self._session_log.append(f"{self._asst_name}: {full_out}")
                                 self._human.observe_assistant(full_out)
+                                try:
+                                    await asyncio.to_thread(
+                                        append_api_turn,
+                                        self._last_user_turn_text,
+                                        full_out,
+                                        session_id=f"jarvis-{self._provider or 'fallback'}",
+                                    )
+                                except Exception as exc:
+                                    print(f"[ChatGPTHistory] Realtime sync skipped: {exc}")
